{
  "name": "mediaseerr",
  "version": "1.0.0",
  "description": "Media Request & Discovery App — vergelijkbaar met Overseerr/Jellyseerr",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo 'Static frontend — no build step needed'"
  },
  "dependencies": {
    "express": "^4.21.0",
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": ">=20"
  }
}
